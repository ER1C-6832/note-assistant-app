"""
Note tool definitions for py-xiaozhi function calling.

Exposes note CRUD operations as tools that the LLM can invoke
based on voice commands, routing through the Notes API.

Tools:
- create_note(title, content, tags?)
- search_notes(query, limit?, ...)
- update_note(id, title?, content?, ...)
- delete_note(id)
- get_note(id)
- list_notes(keyword?, limit?, offset?)
"""

# Placeholder — tool definitions will be implemented in Phase 6.
